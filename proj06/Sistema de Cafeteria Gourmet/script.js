let valorPedido = parseFloat(prompt("Valor do pedido"));
let mensagemBrinde = "";
]if (valorPedido > 50) {
    let temFidelidade = (prompt("Possui Cartão fidelidade? (sim/não)").toLowerCase();
    if (temFidelidade) === "Sim") {
        valorPedido = valorpEDIDO * 0.90;
        mensagemBrinde = "Você ganhou um Cookie de brinde";
    }
    console.log('Total a pagar: R$ ${valorPedido.toFixed(2)}')
;
if (mensagemBrinde) console.(logmensagemBrinde).